# タスクリスト: Unityプロジェクト初期構築

## 実装タスク

- [x] `MiniGameCollection/` ディレクトリ構造を作成する（Unity Hub で作成 + .gitkeep で空フォルダ保持）
- [x] ~~`Packages/manifest.json` を作成する~~ (Unity Hub が自動生成済み)
- [x] ~~`ProjectSettings/` を作成する~~ (Unity Hub が自動生成済み)
- [x] `MiniGameCollection/.gitignore` を作成する（Unity専用除外設定）
- [x] ルートの `.gitignore` に AWSCLIV2.pkg 除外を追加
- [x] ドキュメントの `unity/` → `MiniGameCollection/` パス更新完了

## レビューセクション

（振り返り時に記入）
